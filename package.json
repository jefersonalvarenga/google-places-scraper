{
  "name": "google-maps-scraper",
  "version": "0.0.1",
  "description": "Google Maps scraping Actor using Apify SDK, Crawlee, Playwright, and TypeScript.",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/main.js",
    "lint": "echo \"No lint step configured yet\""
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "apify": "^3.0.0",
    "crawlee": "^3.9.0",
    "playwright": "^1.48.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "files": [
    "dist",
    "apify.json",
    "input-schema.json"
  ]
}
